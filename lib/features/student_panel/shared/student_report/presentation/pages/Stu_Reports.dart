import 'package:flutter/material.dart';

class Stu_Reports extends StatelessWidget {
  const Stu_Reports({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Stu_Reports"),
      ),
      body: const Center(
        child: Text(
          "Stu_Reports",
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
